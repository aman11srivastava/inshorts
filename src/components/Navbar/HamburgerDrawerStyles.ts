import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    list: {
        width: 200,
        paddingLeft: 10,
        paddingRight: 5
    },
    fullList: {
        width: 'auto',
    },
});
